import { Routing } from "pages";
import "./index.scss";
import { withProviders } from "./providers";

const App = () => {
	return (
		<div className="app">
			<Routing />
			ddfsdfsdfsdf
		</div>
	);
};

export default withProviders(App);
